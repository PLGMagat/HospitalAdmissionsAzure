CREATE TABLE hospital_admission(
	Country VARCHAR(50)
	, Indicator VARCHAR(50)
	, [Date] DATE
	, [Year_Week] VARCHAR(50)
	, [Value] FLOAT
	, [Source] VARCHAR(50)
	, [URL] VARCHAR(256)
)

SELECT *
FROM hospital_admission